module.exports = {
  rules: {
    // ...require('./rules/basic').rules,
    ...require('./rules/test').rules,
    // quotes: ['error', 'single', { avoidEscape: true }],
    // 'spaced-comment': 0,
    // 'no-unused-vars': 2,
  },
};
