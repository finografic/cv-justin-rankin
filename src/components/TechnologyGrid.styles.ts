import { css } from '@emotion/react';

export const styles = {
  groupList: css`
    display: grid;
    gap: 1rem;
  `,

  group: css`
    display: grid;
    gap: 0.5rem;
  `,

  note: css`
    margin: 0;
    color: var(--cv-muted);
    font-size: 0.9rem;
  `,
};
