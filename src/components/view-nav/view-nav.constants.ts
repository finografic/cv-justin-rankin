/** Scroll Y (px) above which ViewNav fades out. Tune to taste. */
export const VIEW_NAV_HIDE_AFTER_SCROLL_PX = 40;

/** Opacity fade duration when showing or hiding ViewNav on scroll. */
export const VIEW_NAV_OPACITY_TRANSITION = '1s';
