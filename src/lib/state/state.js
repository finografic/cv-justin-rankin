import React from 'react';
export { ContextGlobal, ContextGlobalProvider, ContextGlobalConsumer } from './ContextGlobal';

const State = props => {
  return React.Fragment; // RETURN NO HTML
};

export default State;
