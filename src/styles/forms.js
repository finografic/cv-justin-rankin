/** @jsx jsx */
// import { Global } from '@emotion/core';
import { css } from '@emotion/core';
import { colors } from './colors';

// LAYOUT VARIABLES
export const forms = {
  inputs: {
    fontSize: '1.1em',
    width: '100%',
    height: '3.33em',
  },
  errors: {
    height: '30px',
  },
};
